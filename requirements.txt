# Install the package in editable mode for development:
#   pip install -e .
#
# Or install pinned runtime deps (mirrors pyproject.toml):
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.28.0
sentencepiece>=0.2.0
numpy>=1.26.0
pandas>=2.0.0
matplotlib>=3.8.0
pyyaml>=6.0.0
tqdm>=4.66.0
modal>=0.64.0
