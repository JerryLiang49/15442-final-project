# Autoregressive baseline (Phase 2). Uses gpt2 by default for a small local/CI-friendly run.
model_name: gpt2
seed: 42
max_new_tokens: 8
device: auto
output_dir: outputs/raw
prompt: "The capital of France is"
torch_dtype: float16
warmup_runs: 1
num_trials: 2
