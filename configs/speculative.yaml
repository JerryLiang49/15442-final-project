# Self-speculative decoding (Phase 3): FP16 draft + FP16 verifier (uncompressed).
model_name: gpt2
seed: 42
max_new_tokens: 16
device: auto
output_dir: outputs/raw
prompt: "Hello, I am"
torch_dtype: float16
spec_k: 3
draft_cache_mode: fp16
