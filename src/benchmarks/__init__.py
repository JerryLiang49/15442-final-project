"""Timing, memory, and structured metrics for decoding experiments.

Import from submodules (e.g. ``from benchmarks.memory import ...``) so
``python -m benchmarks.<module>`` does not load PyTorch via this package alone.
"""
